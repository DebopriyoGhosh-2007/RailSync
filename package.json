{
  "name": "railsync-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix RailSync/frontend run dev",
    "build": "npm --prefix RailSync/frontend run build",
    "lint": "npm --prefix RailSync/frontend run lint",
    "preview": "npm --prefix RailSync/frontend run preview"
  }
}
